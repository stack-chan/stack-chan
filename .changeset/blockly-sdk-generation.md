---
"stack-chan": major
"stackchan-web": major
---

Generate Blockly and face-editor applications through SDK API 2 (host API 8 or later). Events, pending operations and appearance now share the host's AppSession lifecycle. Rebuild old archives from the saved block project; replace retired statement-score blocks with lists of note triples.

Add owned button-release and petting subscriptions, validated song scores and shape-face data, menu navigation and motion hold. Generated applications no longer install their own input dispatchers, timers or raw Piu parts.
