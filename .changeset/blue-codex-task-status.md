---
"stack-chan": minor
---

Show Codex background task activity as an independent blue spinner on the CoreS3 face, and reset it when the USB event transport disconnects.
Reserve the physical CoreS3 USB bridge during host startup so native USBSerial rings are allocated before boot-time memory fragmentation.
Keep application EVENT routing alive with that bridge to retain task snapshots received before MOD activation, while gating context binding and presentation behind conversation activation.
