---
"stack-chan": patch
---

Share public capability names and minimum host API generations across SDK types, metadata validation and Web tools. Reject retired names and unknown profiles. Check actual runtime capabilities before evaluating installed app code and show missing features in recovery. Correct simulator and HTTP capability reporting.
