---
"stack-chan": minor
---

Add an opt-in V2 app SDK with app-owned input handlers, timers, cancellable audio operations and separate natural-language speech and resource playback. V2 apps start independently of boot Wi-Fi and do not inherit legacy app hooks. Add four JavaScript lessons checked against the public SDK and lifecycle tests on XS.
