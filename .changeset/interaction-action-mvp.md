---
"stack-chan": minor
---

Add a host-owned Interaction runtime that composes continuous face animation, emoticons, neck motion, and lighting from semantic events and typed Actions. Default petting and IMU reactions use a typed BehaviorDefinition while existing low-level capabilities remain available as explicit overrides.
