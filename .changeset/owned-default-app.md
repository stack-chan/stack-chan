---
"stack-chan": major
---

Run the built-in behavior through the same SDK app session as installed apps. Add owned menus, semantic input subscriptions, LED operations, delayed tasks and torque release under host API 4. Restore appearance, stop subscriptions and release operations when the app closes.

Remove inherited default hooks from legacy MOD startup. Consolidate the face and localized drawer examples into one SDK example; preserve the three language catalogs. Report missing sensors and lighting as unavailable, including the WASM lighting stub.
