---
"stack-chan": minor
---

Expose a camera hub command capability that executes `tts.speak` through the active TTS provider and maps `panTilt.move` degrees to the configured Stack-chan servo driver.
