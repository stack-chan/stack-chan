# Visual Programming評価手順

## 評価対象

評価対象は、ローカルSDKを使ったことがない参加者です。

参加者ごとに新しいブラウザプロファイルを用意します。

評価者は操作方法を説明せず、チュートリアルの場所だけを伝えます。

## 計測する時点

- エディタを開いた時点
- 最初のビルドに成功した時点
- 最初のシミュレーター実行に成功した時点
- 最初の実機書き込みに成功した時点
- ビルド、USB接続、互換性確認に失敗した時点

エディタはこれらの時点をブラウザ内へ記録します。

評価終了後にヘッダーの評価ログ保存ボタンからJSONを保存します。

## 合格条件

- 15分以内にサンプルの表情か発話を変更し、シミュレーターで再実行できる。
- 対象機種にない能力を配置した場合、実機書き込みより前に理由を確認できる。
- 実行時エラーが発生した場合、対応するブロックへ戻れる。
- 実機書き込み前にチップ、パーティション、サイズ、使用能力を確認できる。
- 書き込み済みMODをバックアップから復元できる。

## 記録

評価ログにはブラウザ識別文字列が含まれます。

公開レポートへ転記する場合は、個人を識別できる拡張機能名や管理情報が含まれていないことを確認します。

複数参加者のログは、最初のシミュレーター実行までの中央値と失敗種別ごとの件数を集計します。

ログを同じディレクトリへ集め、次のコマンドで集計します。

```sh
cd web
npm run metrics:aggregate -- participant-1.json participant-2.json
```

集計結果には、参加者数、15分以内のシミュレーター成功数、最初のシミュレーター実行までの中央値、ビルド失敗数、実機失敗数が含まれます。

## 自動検証の実施記録

2026年7月14日に、実機を使用しない範囲を次の条件で検証しました。

- 単体試験は21スイートすべて成功した。
- 5サンプルを実際にXSAへコンパイルした。
- XSA、プロジェクト、評価ログ、破損データ退避の各ダウンロードについて、ファイル名と内容をブラウザ内で検証した。
- 5 MiBを超えるプロジェクトをIndexedDBへ保存し、ページ再読込後に復元した。
- 破損したIndexedDB状態を退避し、復旧用JSONとして保存できることを確認した。
- 統合シミュレーターでXSAの起動、再起動、停止を行い、2回目の起動まで確認した。
- 統合シミュレーターは、ファームウェアが`[main] app behaviors ready`を出力した後に起動成功を通知することを確認した。
- 統合シミュレーターのAボタンからWASM入力ブリッジを通してMODの入力ハンドラーを起動し、実行時エラーのログ表示と実際に失敗したループブロックの選択を確認した。
- 顔エディターからHOT表情をJSONとして保存し、内容とMIME typeを検証した後、ブロックエディターへ渡してXSAをビルドし、統合シミュレーター上の顔へ反映されることを確認した。
- 外部ESモジュールを改ざんした場合、完全性検査によってシミュレーター起動が拒否されることを確認した。
- エディター、チュートリアル、顔エディター、シミュレーターをデスクトップ幅と390 px幅で確認した。
- 390 px幅ではプロジェクトメニューから新規、複製、対象変更、最近使ったプロジェクト、サンプル、評価ログ、復旧、チュートリアル、全消去へ到達できることを確認した。
- キーボードによるタブ移動とダイアログのEscキー終了を確認した。
- 模擬ローダーで、読戻し照合が失敗した場合に成功を返さず、再起動も行わないことを確認した。
- ファームウェア単体試験141件とアーキテクチャ試験53件が成功した。
- 6ターゲットのmanifest検査と387ファイルのlintが成功した。
- Moddable SDK 8.3.0とEmscripten 5.0.1から、WASM入力ブリッジを含むシミュレーターファームウェアを再生成した。
- Moddable SDK 8.3.0とEmscripten 5.0.1からブラウザ用ビルドツールを再生成し、追跡済み成果物と一致することを確認した。
- ブラウザ用ビルドツールをEmscripten 5.0.1で2回再生成し、`tools.js`のSHA-256が`f24188a0ef7ac675a52724e028e1a8d18436b1b57436702c2797d7aa1d810157`、`tools.wasm`が`8b505e47ecba9039333129ac6a7501e852efed1f4b74f970e6f932739171fd02`で再現することを確認した。
- Web依存関係の監査結果は脆弱性0件だった。
- ファームウェアの実行依存を対象にした監査結果は脆弱性0件だった。

CoreS3実機での書き込み、読戻し照合、復元、削除は未実施です。

評価参加者による15分指標の測定も未実施です。

Webとファームウェアは、開発依存を含む`npm audit`で脆弱性0件です。
